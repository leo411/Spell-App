const NoFav = () => (
  <div className="p-6 max-w-sm mx-auto bg- rounded-xl shadow-lg flex items-center flex-col space-x-4 my-2">
    <span>No spell have been saved</span>
  </div>
);

export default NoFav;
